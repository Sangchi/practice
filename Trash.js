import React from 'react';

const Trash = () => (
  <div style={styles.container}>
    <span>Trash</span>
  </div>
);

const styles = {
  container: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    height: '100%',
  },
};

export default Trash;
